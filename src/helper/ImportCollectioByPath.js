import { testModel } from "../../DataBase/models/test.model";

export const Collections =
{
    USER: "users",
    TEST: testModel.collection.name,
    PRICE: "prices",
    COMPANY: "companies",
    BRANCH: "branches",
    ORDER: "orders",
}